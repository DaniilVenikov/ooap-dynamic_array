Решил, что будет лучше, если убрать у методов remove и put возможность указывать индекс, чтобы работало быстрее,
но почему-то забыл, что в массиве обращение по индексу и так происходит за O(1).

Забыл реализовать метод size, который необходим для удобства.

Совсем намудрил с методом increaseCapacity...

Не догадался, что insert можно заменить на два метода: putRight и putLeft, чтобы иметь большую гибкость.